# AES Domain Proving Ground

Baseline fixtures and deterministic replay inputs for Phase 4 domain templates.

- `*_baseline.json`: expected parity bounds and evidence fields.
- `deterministic_replay_inputs.json`: fixed inputs for reproducible oracle checks.

These fixtures are consumed by `tests/aes_domain_oracles/test_oracle_parity.py`.
